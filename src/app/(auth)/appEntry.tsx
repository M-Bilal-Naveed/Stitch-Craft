import { StyleSheet, Text, View } from "react-native";

const appEntry = () => {
  return (
    <View>
      <Text>appEntry</Text>
    </View>
  );
};

export default appEntry;

const styles = StyleSheet.create({});
